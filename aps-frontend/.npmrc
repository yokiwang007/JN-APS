# npm 配置文件

# 使用淘宝镜像加速(国内)
registry=https://registry.npmmirror.com

# 减少安装超时时间
fetch-timeout=600000

# 禁用严格 SSL(如果遇到证书问题)
# strict-ssl=false

# 减少日志输出
loglevel=warn

# 安装时不保存 package-lock.json(减少文件大小)
# package-lock=false

# 使用更快的安装算法
prefer-offline=true
